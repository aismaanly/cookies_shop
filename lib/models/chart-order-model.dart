// Deklarasi class ChartData
class ChartData {
  // Deklarasi atribut-atribut yang ada dalam ChartData
  final String month; // Bulan (dalam bentuk string)
  final double value; // Nilai (dalam bentuk double)

  // Konstruktor untuk menginisialisasi semua atribut
  ChartData(this.month, this.value);
}
